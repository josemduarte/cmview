This directory contains the master files for the CMView documentation in docbook format.
They can be converted to other formats (HTML, PDF, JavaHelp) using XSLT stylesheets
(see generate_docs.sh, publish_to_web.sh).

To edit docbook in eclipse we have used the Vex Plugin (http://wiki.eclipse.org/Vex).